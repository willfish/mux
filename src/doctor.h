#ifndef MUX_DOCTOR_H
#define MUX_DOCTOR_H

/* Check that all required dependencies are available.
 * Prints status to stdout. Returns 0 if all OK, -1 if something is missing. */
int doctor_check(void);

#endif
